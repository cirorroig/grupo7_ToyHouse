<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head") %>
<body>
    <%- include("./partials/header") %>
    <main class="login-page">
        <h1>¡Inicia sesion!</h1>
        <form action="/users/login" method="post" class="login-form"id="form">
            <div class="information">
                <label for="email">Correo electronico</label>
                <input id="email" type="email" name="email" placeholder="tu@mail.com"> 
                <% if (locals.errors && errors.email) { %>                
                    <div class="text-danger">
                    <%= errors.email.msg %> 
                    </div>
                <% } %>
                <small class="emailError error">Ingresa un  email valido</small>
            </div>
            
            <div class="information">
                    <label for="password">Contraseña</label>
                    <input id="password" type="password" name="password" id="password"> 
                    <small class="emailError error">Ingresa una contraseña valida</small>
                    <small class="passwordError error">La contraseña debe tener al menos 8 caracteres, 1 mayúscula, 1 minúscula, 1 número y un caracter especial</small>
            </div>
        
            </div>
            <div class="remember">
                
                <label for="remember"> <input type="checkbox" name="rememberUser">Recordarme</label>
            </div>        
            <div class="login">
                <button type="submit" class="login">Inicia sesion</button>
            </div>
        </form>
    </main>
        <%- include("./partials/footer")%>
        <%- include("./partials/scripts")%> 
    <script src="/js/login.js"></script>
</body>
</html>