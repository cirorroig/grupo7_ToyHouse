<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head") %>
<body>
    <%- include("./partials/header")%>
    <main class="product-detail">
        <h1> 
            <%=product.name%>
        </h1>
        <section>
        <div class="img">
            <img src="../images/Products/<%=product.image%>" alt="product">
        </div>
        <div class="info">
          <div class="information">
            <p><%=product.detailedDescription%></p>
            <ul>
                <li>Este producto esta recomendado para niños y niñas <%=product.edad.range%></li>
                <li>Este producto pertenece a la categoria de <%=product.categoria.name%></li>
                <% if (product.talle) { %>
                 <li> El talle de este producto es <%=product.talle.talle%></li>
                 <% } %>                
            </ul>
        </div>
       
        <div class="detail-buy-button">
            <a href="/cart"><button type="submit" class="buy">Comprar</button></a>
             <% if (locals.isLogged && locals.userLogged.is_admin==1) { %>
            <a href="/products/<%= product.id_producto %>/edit"><button class="edit">Editar Producto</button></a>
            <% } %>        
        </div>   
        </div>       
        </section>
        
    </main>
       <%- include("./partials/footer")%>
</body>
</html>